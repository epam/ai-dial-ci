name: Run e2e-tests

on:
  repository_dispatch:
    types: [e2e-tests-workflow]

jobs:
  deploy-env:
    runs-on: ubuntu-latest
    environment:
      name: e2e-test
    outputs:
      github-app: ${{ github.event.client_payload.github-app }}
      github-sha: ${{ steps.sha.outputs.SHA }}
    steps:
      - name: Obtain short sha
        id: sha
        shell: bash
        env:
          SOURCE_COMMIT_SHA: ${{ github.event.client_payload.github-sha }}
        run: |
          # shorten the commit SHA to the first 7 characters
          echo "SHA=${SOURCE_COMMIT_SHA:0:7}" >>$GITHUB_OUTPUT
      - name: Deploy environment
        id: deploy
        uses: digital-blueprint/gitlab-pipeline-trigger-action@c59b56e9d2688ab42c1304322ac8831a4ef6f7d2 # v1.4.0
        with:
          host: ${{ vars.DEPLOY_HOST }}
          trigger_token: ${{ secrets.DEPLOY_TRIGGER_TOKEN }}
          access_token: ${{ secrets.DEPLOY_ACCESS_TOKEN }}
          id: ${{ github.event.client_payload.gitlab-project-id }}
          ref: ${{ github.event.client_payload.gitlab-project-ref || vars.DEPLOY_REF }}
          download_artifacts_on_failure: false
          verbose: true
          variables: >
            {
              "APPLICATION_NAME":"${{ github.event.client_payload.github-app }}",
              "SOURCE_COMMIT_SHA": "${{ steps.sha.outputs.SHA }}",
              "ENVIRONMENT_ACTION": "deploy"
            }

  e2e-test:
    uses: epam/ai-dial-ci/.github/workflows/gh-e2e-test.yml@4.0.1
    needs:
      - deploy-env
    with:
      report-prefix: "${{ needs.deploy-env.outputs.github-app }}-${{ needs.deploy-env.outputs.github-sha }}"
      e2e-host: "https://chat-${{ needs.deploy-env.outputs.github-app }}-${{ needs.deploy-env.outputs.github-sha }}.${{ vars.E2E_BASE_DOMAIN }}"
      next-public-overlay-host: "https://overlay-${{ needs.deploy-env.outputs.github-app }}-${{ needs.deploy-env.outputs.github-sha }}.${{ vars.E2E_BASE_DOMAIN }}"
      environment-name: e2e-test
    secrets:
      E2E_ADMIN: ${{ secrets.E2E_ADMIN }}
      E2E_USERNAME: ${{ secrets.E2E_USERNAME }}
      E2E_OVERLAY_USERNAME: ${{ secrets.E2E_OVERLAY_USERNAME }}
      E2E_PASSWORD: ${{ secrets.E2E_PASSWORD }}
      NEXT_PUBLIC_OVERLAY_USER_BUCKET: ${{ secrets.NEXT_PUBLIC_OVERLAY_USER_BUCKET }}

  destroy-env:
    runs-on: ubuntu-latest
    environment:
      name: e2e-test
    needs:
      - deploy-env
      - e2e-test
    if: ${{ !cancelled() }}
    steps:
      - name: Destroy environment
        id: destroy
        uses: digital-blueprint/gitlab-pipeline-trigger-action@c59b56e9d2688ab42c1304322ac8831a4ef6f7d2 # v1.4.0
        with:
          host: ${{ vars.DEPLOY_HOST }}
          trigger_token: ${{ secrets.DEPLOY_TRIGGER_TOKEN }}
          access_token: ${{ secrets.DEPLOY_ACCESS_TOKEN }}
          id: ${{ github.event.client_payload.gitlab-project-id }}
          ref: ${{ github.event.client_payload.gitlab-project-ref || vars.DEPLOY_REF }}
          download_artifacts_on_failure: false
          verbose: true
          variables: >
            {
              "APPLICATION_NAME":"${{ needs.deploy-env.outputs.github-app }}",
              "SOURCE_COMMIT_SHA": "${{ needs.deploy-env.outputs.github-sha }}",
              "ENVIRONMENT_ACTION": "destroy"
            }
